# ── Data sources ──────────────────────────────────────────────────────────────
pybaseball>=2.2.5
python-mlb-statsapi>=0.7.2
sbrscrape>=0.1.0
openmeteo-requests>=1.2.0
requests-cache>=1.1.0          # required by openmeteo-requests
retry-requests>=2.0.0          # required by openmeteo-requests

# ── Kalshi & Polymarket ───────────────────────────────────────────────────────
pykalshi>=1.0.0                 # unofficial Kalshi client — better DX than official
requests>=2.31.0
httpx>=0.25.0                   # async HTTP for Kalshi WebSocket
websockets>=12.0

# ── ML & statistics ───────────────────────────────────────────────────────────
scikit-learn>=1.4.0
scipy>=1.12.0                   # Poisson/NegBinom distributions, convolution
statsmodels>=0.14.0             # NegativeBinomial GLM if overdispersion confirmed
numpy>=1.26.0
pandas>=2.1.0
joblib>=1.3.0                   # model serialisation

# ── Hyperparameter tuning & explainability ────────────────────────────────────
optuna>=3.5.0                   # Bayesian hyperparameter optimisation
shap>=0.44.0                    # feature importance

# ── Database ──────────────────────────────────────────────────────────────────
# SQLite is stdlib — no package needed

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0

# ── Code quality ─────────────────────────────────────────────────────────────
ruff>=0.3.0

# ── Notebooks & visualisation ────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.29.0
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0                  # interactive backtest charts

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0            # load .env for API keys
pyyaml>=6.0                     # load configs/model_config.yaml
openpyxl>=3.1.0                 # fallback Excel parsing if needed
tqdm>=4.66.0                    # progress bars for long scrape loops
